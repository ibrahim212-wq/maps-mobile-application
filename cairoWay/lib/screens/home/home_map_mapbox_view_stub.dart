import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

/// Web stub — real implementation never built when `kIsWeb` (see [HomeMapView]).
class HomeMapMapboxView extends ConsumerStatefulWidget {
  const HomeMapMapboxView({super.key});

  @override
  ConsumerState<HomeMapMapboxView> createState() => _HomeMapMapboxViewStubState();
}

class _HomeMapMapboxViewStubState extends ConsumerState<HomeMapMapboxView> {
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
